using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.Reflection.Metadata;
using System.Text;
using System.Windows.Markup;

namespace AcademyCoding
{
    class ToJSON
    {
        public static void ExportJSONFile()
        {
            //List <Dictionary<string, string >> values = JsonConvert.DeserializeObject<List<Dictionary<string, string>>>(texto);
            // List<string> keyList = new List<string>(values[0].Keys);
            //List<string> valuesList = new List<string>(values[0].Values);
            // Console.WriteLine(keyList[0]); 
        }
    }
}
